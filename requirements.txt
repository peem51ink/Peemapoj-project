# ─────────────────────────────────────────────────────────────────────
# requirements.txt
# Project: Wildfire Smoke Detection from Drone Images
# Dataset: Sri Lanna Grid Annotation Dataset
# Task:    Binary classification — Smoke vs No Smoke (image-level)
# ─────────────────────────────────────────────────────────────────────

# ── Core Data Science ────────────────────────────────────────────────
numpy>=1.24.0
pandas>=2.0.0

# ── Machine Learning ─────────────────────────────────────────────────
scikit-learn>=1.3.0        # train_test_split, metrics, classification_report

# ── Visualization ────────────────────────────────────────────────────
matplotlib>=3.7.0          # plots, ROC/PR curves, confusion matrix
seaborn>=0.12.0            # EDA heatmaps, distribution plots

# ── Image Processing (Lesson 4 — Transfer Learning) ─────────────────
Pillow>=10.0.0             # Image.open(), convert("RGB")

# ── Deep Learning (ResNet / EfficientNet — next lesson) ─────────────
torch>=2.0.0
torchvision>=0.15.0        # transforms, pretrained models (ResNet, EfficientNet)

# ── Jupyter Notebook ─────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
